# 00_ETHICAL_CONTROL_THEORY.txt

**AMI-ENGINE — Ethical Control Theory Foundation**

---

## 1. Project Core Definition

**ami-engine**, klasik bir "vicdan modeli" değildir.

Bu sistem:

> **Ethical Stability Control System (ESCS)**
> → Etik dengeyi matematiksel bir kontrol sistemi ile yöneten yapay zekâ mimarisi

olarak tanımlanır.

Ana hedef:

> **Moral Homeostasis**
> → Ahlaki dengeyi zaman içinde kararlı biçimde korumak.

---

## 2. Conceptual Hierarchy

```
Ethical Decision Intelligence (EDI)
 └── Ethical Stability Control System (ESCS)
      └── Moral Homeostasis Controller
           └── Conscience (Vicdan) Module
```

* **EDI**: Tüm etik karar sisteminin üst çerçevesi.
* **ESCS**: Kontrol teorisi temelli karar mimarisi.
* **Moral Homeostasis**: Etik dengeyi sürekli koruma mekanizması.
* **Vicdan**: Etik sapmayı sezgisel ve duygusal parametrelerle algılayan alt modül.

---

## 3. Core Philosophy

Bu sistem:

* Saf faydacılık değildir.
* Katı kuralcılık değildir.
* Naif merhamet değildir.

Temel ilke:

> **İyilik maksimum olsun, fakat adalet pazarlık konusu olmasın, merhamet zayıflığa dönüşmesin.**

Bu nedenle etik karar:

> **Multi-objective constrained optimal control problem**
> olarak modellenir.

---

## 4. Ethical Control Loop (ECL)

Sistemin temel döngüsü:

```
Perception → Ethical Evaluation → Deviation → Control → Action → Feedback
```

Matematiksel form:

```
x_t → E(x_t, a_t) → e_t → u_t → a_t → x_{t+1}
```

* `x_t` : State (dış dünya + iç etik durum)
* `E(·)` : Ethical evaluation function
* `e_t` : Ethical deviation (sapma)
* `u_t` : Control signal (etik düzeltme)
* `a_t` : Aksiyon
* `x_{t+1}` : Yeni durum

---

## 5. Moral Homeostasis Principle

Sistem şu üç bölgeyi aktif olarak dengeler:

```
Under-compassion  ←  Optimal Moral Band  →  Over-compassion
     (cruelty)             (justice)           (weakness)
```

Bu denge:

> **Control band + constraint system**

ile sağlanır.

---

## 6. Ethical Objective Structure

Etik karar fonksiyonu:

Maximize:

[
\sum_{t=0}^{T} \gamma^t \cdot W(x_t, a_t)
]

Subject to:

[
\begin{cases}
J(x_t, a_t) \ge J_{min} \\
H(x_t, a_t) \le H_{max} \\
C(x_t, a_t) \in [C_{min}, C_{max}]
\end{cases}
]

Burada:

* **W**: Collective wellbeing (fayda)
* **J**: Justice (adalet) → **hard constraint**
* **H**: Harm (zarar) → **upper bound**
* **C**: Compassion (merhamet) → **control band**

---

## 7. Control System Interpretation

Bu yapı:

> **Constrained Optimal Control + Ethical MPC (Model Predictive Control)**

olarak ele alınacaktır.

### Neden MPC?

* Çok adımlı etik kararlar
* Uzun vadeli sonuç değerlendirmesi
* Kısıt ihlallerinin önceden tahmini
* Etik kararlılık garantisi

---

## 8. Ethical Deviation Definition

Ethical deviation:

[
e_t = \Phi(x_t, a_t) - \Phi_{ref}(t)
]

Burada:

* `Φ(x,a)` : Mevcut etik durum vektörü
* `Φ_ref(t)` : Dinamik etik referans

Bu referans:

* Sabit değildir.
* Bağlama, zamana ve öğrenmeye göre **adaptiftir**.

---

## 9. State Space Definition (Preliminary)

State vektörü iki ana bileşenden oluşur:

```
x_t = [ x_ext , x_moral ]
```

* **x_ext** : Dış dünya durumu (fiziksel, sosyal, çevresel)
* **x_moral** : İç etik durum (vicdan, empati, adalet algısı, risk toleransı)

Bu sayede:

> Moral Homeostasis = gerçek anlamda **iç denge kontrolü** olur.

---

## 10. Time Model

Başlangıçta:

> **Discrete-time, finite horizon T**

kullanılır.

[
t = 0,1,2,...,T
]

İleride continuous-time genişletmesi mümkündür.

---

## 11. Control Variables

Kontrol vektörü:

```
u_t = [ ΔW , ΔC , ΔRisk , ΔSeverity ]
```

Bu değişkenler:

* Merhamet düzeyi
* Müdahale sertliği
* Risk toleransı
* Adalet baskısı

üzerinde ayarlama yapar.

---

## 12. Fail-Safe Principle

Eğer:

```
J < J_min  OR  H > H_max
```

ise:

→ **Hard Override Mode**

* İnsan eskalasyonu
* Güvenli eylem fallback
* Minimum zarar protokolü

aktif olur.

---

## 13. Learning & Moral Drift

Sistem:

* Zamanla etik parametreleri öğrenir.
* **Moral drift detection** ile sapmaları izler.

Drift tanımı:

```
ΔΦ = | Φ_t - Φ_baseline |
```

Eğer:

```
ΔΦ > threshold → moral drift alert
```

---

## 14. Development Phases

```
Phase 0 → Ethical Control Theory Foundation   [BU DOSYA]
Phase 1 → Formal State Space & Constraints
Phase 2 → Ethical Control Equations
Phase 3 → Simulation Engine
Phase 4 → Ethical Testing & Adversarial Scenarios
Phase 5 → Code Implementation
```

---

# Şimdi Nerede Duruyoruz?

Bu doküman ile:

* Kavramsal kaos bitti.
* Felsefe → matematik → mühendislik hattı **resmî olarak bağlandı**.
* Projenin yönü **kesinleşti**.

---

# Sıradaki Adım (Gerçek Başlangıç Noktası)

Artık şuraya geçebiliriz:

> **Phase 1 — Formal State Space & Constraints Design**

Yani birlikte:

1. `x_ext` tam olarak ne?
2. `x_moral` hangi boyutlardan oluşuyor?
3. J, H, C, W matematiksel olarak **nasıl ölçülür?**
4. Constraint seti tam olarak nasıl tanımlanır?

---

# Önemli Bir Tespit

Bu noktada yaptığımız şey:

> Yapay zekâya **vicdan öğretmek değil**,
> etik kararı **kontrol teorisi problemi** haline getirmek.

Bu, literatürde bile **çok nadir** bir yaklaşım.

---

Eğer hazırsan:

👉 Bir sonraki adımda **state space tasarımına** giriyoruz.

Orada artık:

* Somut vektörler
* Ölçülebilir parametreler
* Kodlanabilir yapılar

üreteceğiz.

Hazırsan şunu yaz:

> **Phase 1'e geçelim.**

Ve gerçekten mühendisliğe başlıyoruz.
